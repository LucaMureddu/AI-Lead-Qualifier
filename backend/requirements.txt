# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── LangGraph + LangChain ─────────────────────────────────────────────────────
langgraph>=0.2.0
langgraph-checkpoint-postgres>=0.1.0   # AsyncPostgresSaver (replaces sqlite in V2)
langchain-core>=0.2.0

# ── LLM backends ──────────────────────────────────────────────────────────────
llama-cpp-python>=0.2.80
groq>=0.9.0
langchain-community>=0.2.0
langchain-ollama>=0.1.0                # OllamaEmbeddings (sostituisce langchain_community)

# ── Async HTTP ────────────────────────────────────────────────────────────────
httpx>=0.27.0

# ── PostgreSQL + pgvector (V2) ────────────────────────────────────────────────
asyncpg>=0.29.0
pgvector>=0.2.0                        # Python type hints for pgvector

# ── Redis / ARQ task queue (V2) ───────────────────────────────────────────────
arq>=0.26.0
redis>=5.0.0

# ── Structured logging (V2) ───────────────────────────────────────────────────
structlog>=24.0.0

# ── Settings & validation ─────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Auth / JWT RS256 (V2) ─────────────────────────────────────────────────────
PyJWT>=2.8.0
cryptography>=42.0.0                   # RSA key loading for JWT RS256

# ── Migrazioni DB (V2) ───────────────────────────────────────────────────────
alembic>=1.13.0
sqlalchemy>=2.0.0                      # usato solo da alembic/env.py (non ORM runtime)
psycopg2-binary>=2.9.0                 # driver sincrono per Alembic (asyncpg resta per runtime)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# ── Object Storage S3 (V2.1) ──────────────────────────────────────────────────
# aioboto3: wrapper asincrono di boto3 per Python asyncio (MinIO in dev, AWS S3 in prod).
# types-aiobotocore-s3: stub MyPy per type-check del client S3.
# botocore è dipendenza transitiva di aioboto3.
aioboto3>=13.0.0
types-aiobotocore-s3>=2.13.0

# ── Rate limiting (V2.1) ──────────────────────────────────────────────────────
# slowapi: rate limiting per FastAPI/Starlette.
# limits[redis]: backend Redis per slowapi (fail-open se Redis non risponde).
slowapi>=0.1.9
limits[redis]>=3.13.0

# ── Dev / quality ─────────────────────────────────────────────────────────────
ruff>=0.4.0
mypy>=1.10.0
